{
  "annotations": {
    "list": []
  },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "ops"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 0
      },
      "id": 1,
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (result) (rate(api_collector_kafka_publish_events_total[5m]))",
          "legendFormat": "{{result}}",
          "range": true,
          "refId": "A"
        }
      ],
      "description": "五分钟窗口内每秒 Kafka 投递事件数；投递成功不等于消费端已写库。",
      "title": "Collector Kafka 投递结果",
      "type": "timeseries"
    },
    {
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 0
      },
      "id": 2,
      "targets": [
        {
          "editorMode": "code",
          "expr": "histogram_quantile(0.95, sum by (le, biz_type, strategy, result) (rate(api_idgen_generate_duration_seconds_bucket[5m])))",
          "legendFormat": "{{biz_type}} / {{strategy}} / {{result}}",
          "range": true,
          "refId": "A"
        }
      ],
      "description": "按业务类型、发号策略和结果汇总的 P95 发号耗时，单位为秒。",
      "title": "ID 生成耗时 P95",
      "type": "timeseries"
    },
    {
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 8
      },
      "id": 3,
      "targets": [
        {
          "editorMode": "code",
          "expr": "histogram_quantile(0.95, sum by (le, biz_type, result) (rate(api_idgen_segment_allocation_duration_seconds_bucket[5m])))",
          "legendFormat": "{{biz_type}} / {{result}}",
          "range": true,
          "refId": "A"
        }
      ],
      "description": "向 Redis 申请新号段的 P95 耗时，单位为秒，不含本地取号。",
      "title": "ID Segment 申请耗时 P95",
      "type": "timeseries"
    },
    {
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "ops"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 8
      },
      "id": 4,
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (biz_type, event) (rate(api_idgen_snowflake_lease_events_total[5m]))",
          "legendFormat": "{{biz_type}} / {{event}}",
          "range": true,
          "refId": "A"
        }
      ],
      "description": "按事件类型统计 Snowflake 节点租约活动，不能作为业务发号吞吐。",
      "title": "Snowflake 租约事件",
      "type": "timeseries"
    },
    {
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "ops"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 16
      },
      "id": 5,
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (biz_type, strategy, result) (rate(api_idgen_generated_total[5m]))",
          "legendFormat": "{{biz_type}} / {{strategy}} / {{result}}",
          "range": true,
          "refId": "A"
        }
      ],
      "description": "按业务类型、发号策略和结果汇总每秒发号次数。",
      "title": "ID 生成吞吐",
      "type": "timeseries"
    },
    {
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 16
      },
      "id": 6,
      "targets": [
        {
          "editorMode": "code",
          "expr": "api_idgen_segment_remaining",
          "legendFormat": "{{biz_type}}",
          "range": true,
          "refId": "A"
        }
      ],
      "description": "各实例当前本地号段尚可使用的 ID 个数，不是 Redis 全局余额。",
      "title": "ID Segment 本地剩余 ID 数",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": [
    "api",
    "auth",
    "collector",
    "idgen"
  ],
  "templating": {
    "list": [
      {
        "current": {
          "selected": false,
          "text": "Prometheus",
          "value": "Prometheus"
        },
        "hide": 0,
        "includeAll": false,
        "label": "Prometheus",
        "multi": false,
        "name": "DS_PROMETHEUS",
        "options": [],
        "query": "prometheus",
        "refresh": 1,
        "regex": "",
        "type": "datasource"
      }
    ]
  },
  "time": {
    "from": "now-6h",
    "to": "now"
  },
  "timezone": "browser",
  "title": "api 认证、Collector 与 ID",
  "uid": "api-auth-collector",
  "version": 1
}
