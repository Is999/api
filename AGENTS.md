# AGENTS.md

本文件是所有 AI Agent 参与 `api-go` 开发的仓库入口。修改代码、配置、SQL、脚本或文档前，必须完整阅读：

- `docs/site/角色文档/后端开发/AI开发提示词.md`
- `docs/site/角色文档/后端开发/AI开发规范.md`

不能直接识别仓库 `.agents/skills` 的 AI Agent，必须先将当前任务需要的 Skill 转换为自身支持的 rule、skill 或 instruction 格式；转换不得省略触发条件、确认闸门、禁止项、引用资源和验证命令。转换规则见 `.agents/skills/standard-development-flow/references/team-adoption.md`。

## 最高优先级：专业实现、低门槛维护

- 业务正确、安全、数据完整性和资源边界不可退让；在此前提下，代码简洁、逻辑清晰、易阅读易维护是所有实现的最高优先级，不是功能跑通后的可选润色。
- 实现必须达到专业资深的高级开发者标准，初级开发者也应能通过主流程、就近注释和测试理解已实现的需求与业务规则、定位修改及回归；按 [AI 开发规范](docs/site/角色文档/后端开发/AI开发规范.md) 验收，不能只以资深开发者看得懂或测试通过放行。

## 禁止概括性规则

- “简洁、清晰、闭环、同步、必要时、相关、验证通过、生产可用”等词不能单独作为实施要求或完成结论；必须按 AI 开发规范补出触发条件、检查对象、通过标准、禁止项和证据。
- “闭环”必须核对真实入口、主调用链、成功/失败分支、数据或外部副作用、注册与配置、同步面、测试和运维后续；helper、脚手架、DTO、单一 happy path 或孤立测试不能证明完成。
- “同步”必须先按源码变化和调用方搜索识别触发面，再记录源码依据、实际修改文件和验证结果；route、request/response、RouteMeta、安全策略、业务码/i18n、配置、Model、初始化 DDL/DML、SQL/Lua、Redis Key、前端契约、文档、测试和运维步骤只作为发现清单，不能变成机械填表。
- “验证通过”必须记录最后一次修改后的命令、执行目录、范围和结果；旧快照、只测 helper 或只编译不能证明整项需求完成。
- 分析 bug、阻塞点或测试失败时，必须分栏输出“正常逻辑缺陷”“极端运行条件”和“验证环境阻塞”。生产事件或主动故障注入已经执行到依赖故障处理代码时归入极端运行条件；验证原本要求依赖健康，但本机环境在到达待测逻辑前失败时归入验证环境阻塞。极端运行条件栏必须继续标明代码处理符合预期或存在韧性缺陷；每项列出触发条件、证据、期望/实际行为、影响、代码责任和处理结论，禁止把依赖不可达、代码判断错误和本机缺工具混合列举或合并计数。
- 服务运行路径禁止调用 `panic`、运行期 `Must*`、`log.Fatal*`、logger `Panic*`、`os.Exit`、`syscall.Exit`、`runtime.Goexit` 或等价致命能力。错误必须返回到可控边界，由请求、任务或组件入口记录上下文并执行降级、重试或拒绝；只允许 `cmd/*` 主入口在尚未监听，或全部监听器和后台组件已经停止且资源清理完成后，把最终进程结果转换为退出码，失败结果必须非零。

## 基础骨架保护

- 本仓库承担前台 API 基础骨架。业务开发、框架演进、重构或跨仓同步可能触及启动、配置、HTTP/RPC、身份安全、错误信封、数据访问、collector/worker、资源或构建底座时，必须先使用 `$foundation-baseline-guard` 区分领域业务、业务接入和核心变更。
- 核心变更必须说明框架缺陷证据、领域层不可解决原因、跨服务契约影响、替代方案、验证和回退方式，并取得开发人员明确确认；一般业务授权不能替代核心变更确认。

## 执行规则

1. 先确认用户要求属于分析、实现、审查、验证还是交接；先读现有目录、代码、调用链、测试和相关文档，再修改文件。
2. 需求含糊、存在多种解释、目标入口不清，或触及 API、数据、权限、安全、大表、缓存、迁移和补偿时，先说明理解、源码证据、执行边界与待确认点；未经确认不得猜测实现。
3. 优先延续 `api-go` 现有分层，并与同工作区 `admin-go` 保持共享契约一致。新增抽象、目录、框架或依赖前，说明必要性、替代方案、影响范围和验证方式，并取得开发确认。
4. 每个修改文件和关键 diff 都必须映射到需求项、必要同步面或验证修复；无映射的重命名、格式化、目录调整和顺手重构不得混入本任务，也不得回滚用户已有改动。
5. 每个新增 helper、接口、包装层、状态和分支都必须能指出当前需求、真实调用方、稳定复用边界或故障证据；只转发参数且没有校验、转换、事务、重试或依赖隔离职责的包装应内联。
6. 目录、文件、变量、方法、常量、结构体、接口和字段使用短而准确的业务名称；职责变化时同步改名。
7. 新增或重构的变量、常量、方法、结构体、字段、map 字段、路由、GORM Model、接口和关键逻辑补中文注释。方法内出现多阶段 I/O、事务/锁/CAS、状态迁移、重试/降级、批处理、安全顺序或资源清理时，必须在对应逻辑块前用简短中文说明业务原因、执行顺序和失败边界；一个逻辑块对应一条就近注释，多个逻辑不得在方法开头或单处合并说明，一句话能说清楚时不得写成长段。不得给普通赋值、显然条件和统一错误返回逐行翻译。新增或修改的项目自有 YAML 固定配置字段必须在字段正上方以相同缩进补中文注释，至少说明消费组件和用途；源码存在枚举/范围/单位、缺省/空值/零值、normalize/getter、热加载/重启、敏感信息或跨字段约束时逐项写明。父节点概述不能替代子字段注释；动态 map 重复数据项只有在父字段已定义 key/value、空值和合并语义时才可不重复注释；第三方 schema、纯数据清单或生成文件必须记录具体排除依据。
8. 接口或行为变化时，先按源码 diff 和调用方搜索识别触发面。route、request/response、RouteMeta、安全字段、业务码/i18n、配置、Model、初始化 DDL/DML、SQL/Lua、Redis Key、前端 wrapper/type、文档、测试和运维步骤是发现清单，不是固定填空表；每个触发面记录源码依据、修改文件和验证结果。只有常见风险面容易被误判为已处理时才写不适用及具体依据，禁止用一个“不适用”概括所有未检查项。
9. MySQL 默认使用 GORM 链式调用；无跨语句原子性或一致性快照要求时不额外包事务。必须用事务时独立查询、校验、计算和整理前置，锁定读取及提交重验保留；事务内不做远程 I/O、文件处理或等待，按 AI 规范核对行数、执行时间、锁等待和原子性，禁止无界大事务、长事务。原生 SQL 仍须说明 GORM 例外及索引、扫描、批量、超时和并发依据。
10. 大表、大结果集和高频查询先评估索引、执行计划、返回规模与并发；禁止在线接口执行无边界查询或高基数 Redis 通配扫描。
11. 仓库只维护完整初始化 DDL/DML；需求变更直接修改原始资产，不新增碎片化迁移 SQL 或日常迁移版本。迁移命令允许非空库执行未登记的幂等资产，已登记项校验后跳过；执行权限与元数据校验不得绕过。存量库增量 SQL 只放被忽略的 `data/sql-changes/<change-id>/`，交 DBA/运维执行，禁止提交或暂存；登记版本差异不能单独成为服务启动失败条件。

## Skill 路由

非平凡开发、修复、重构、生产复核和交付前检查必须先使用 `$standard-development-flow`，再按场景使用专项 skill：

| 场景 | 必用 skill |
| --- | --- |
| 需求含糊或高风险边界 | `$requirement-intent-gate` |
| 业务开发或框架演进可能触及 API 基础骨架 | `$foundation-baseline-guard` |
| Go 实现、修复或重构 | `$go-ai-implementation-guard` |
| 代码优化、简化或性能复核 | `$simple-code-guard` |
| 全仓注释与可维护性审计 | `$go-comment-style-audit` |
| API、路由、业务码或安全字段变化 | `$api-contract-sync` |
| Redis Key、TTL、失效或扫描治理 | `$redis-key-governance` |
| GORM、SQL、索引或大查询 | `$go-db-query-performance-guard` |
| 测试、vet、race、构建或 CI 失败 | `$go-test-failure-debugger` |
| 配置、默认值、热加载或重启边界 | `$go-config-runtime-guard` |
| Worker、Collector、锁、重试或并发状态 | `$go-worker-concurrency-guard` |
| JWT、session、权限、签名或加密 | `$go-security-route-guard` |
| DDL、Model、迁移、回填或补偿 | `$go-migration-backfill-runbook` |
| 兄弟仓库比较与同步 | `$sibling-repo-sync-review` |
| 需求闭环、命名和交付完整性 | `$implementation-closure-review` |
| 最终验证、暂存与运维交接 | `$release-handoff-guard` |

## 必跑检查

- Go 改动优先运行 `go test ./...`；范围很小或环境受限时至少运行相关包测试，并说明未跑全量的原因。
- 每轮结束前在本仓库运行 `git diff --check` 和 `git status --short`；存在 staged 文件时再运行 `git diff --cached --check`。
- 只暂存本轮新增且属于交付范围的文件，不添加本地数据、构建产物或历史无关文件。

## 交付要求

最终说明必须列出修改内容、同步面、验证命令与结果、跳过项和剩余风险，并明确是否需要迁移、回填、缓存失效、重启、回滚或补偿。
